# 01. 소프트웨어 요구사항 명세서 (SRS)

**PaperKo — 학술 논문 PDF 한국어 번역 데스크탑 앱**

## 1. 개요

### 1.1 목적

본 문서는 학술지 논문 PDF를 원본 레이아웃을 유지한 채 한국어로 번역하는 데스크탑 애플리케이션의 기능·비기능 요구사항을 정의한다. 개발자, 테스터가 공유하는 단일 기준(single source of truth)으로 사용한다.

### 1.2 범위

- **포함**: PDF 열기·미리보기, 레이아웃 분석, 로컬 LLM(vLLM 서버) 기반 번역, 전문용어 사전 관리, 번역 PDF 재조판·저장, 원문/번역 대조 뷰.
- **제외(v1)**: 클라우드 상용 번역 API 연동, PDF 편집기 기능, DRM 걸린 PDF 해제, 모바일 버전, 다국어 타깃(한국어 외).

### 1.3 대상 사용자

- 주 사용자: 영어 논문을 한국어로 빠르게 읽고 싶은 대학원생·연구자·교수.
- 특성: 전문용어의 일관된 번역과 수식·그림·표의 원형 유지를 중시. 오프라인/내부망 환경(연구실 서버의 vLLM) 사용.

### 1.4 운영 환경

| 항목 | 내용 |
|---|---|
| OS | Windows 10+, macOS 13+, Linux (x86-64) |
| 메모리 | 최소 8 GB (권장 16 GB) — 레이아웃 분석 모델 로딩 포함 |
| 네트워크 | vLLM 서버 도달 가능(내부망/로컬). 그 외 외부 네트워크 불필요 |
| LLM 서버 | vLLM, OpenAI 호환 API, 모델 `qwen3.6-35B-A3B-NVFP4-fast` |

## 2. 사용자 시나리오 (주요 유스케이스)

### UC-01 논문 번역 (핵심 흐름)

1. 사용자가 PDF 파일을 드래그&드롭 또는 파일 선택으로 연다.
2. 앱이 파싱·레이아웃 분석을 수행하고 페이지 미리보기와 블록 오버레이를 표시한다.
3. 사용자가 번역 범위(전체/페이지 범위), 용어집, 옵션(수식 보존 등)을 선택하고 [번역 시작]을 누른다.
4. 진행률(페이지/블록 단위)이 실시간 표시되고, 완료된 페이지부터 번역 미리보기가 갱신된다.
5. 완료 후 사용자가 결과를 검토하고 PDF로 저장한다.

### UC-02 전문용어 사전 관리

1. 사용자가 용어집 화면에서 용어쌍(예: `attention → 어텐션`)을 추가/수정/삭제한다.
2. CSV/TSV 가져오기·내보내기를 지원한다.
3. 번역 시 선택한 용어집이 프롬프트에 반영되고, 후처리에서 강제 적용 여부를 검증한다.

### UC-03 부분 재번역

1. 사용자가 번역 결과에서 특정 블록을 선택해 [다시 번역]을 누른다.
2. 해당 블록만 재번역되어 교체된다. 사용자가 직접 수정한 텍스트도 저장에 반영된다.

### UC-04 스캔 PDF 처리 (OCR)

1. 텍스트 레이어가 없는(스캔본) 페이지를 앱이 자동 감지한다.
2. 사용자 동의 후 OCR을 수행하여 이후 파이프라인은 동일하게 진행한다.

## 3. 기능 요구사항

표기: **M**(Must, v1 필수) / **S**(Should, v1 목표) / **C**(Could, v1.x)

### 3.1 문서 입력·파싱 (FR-1x)

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-10 | M | PDF 1.4~2.0 파일을 열고 페이지 수·크기·텍스트 레이어 유무 등 메타데이터를 표시한다 |
| FR-11 | M | 텍스트, 폰트/크기/스타일, 좌표(bbox), 이미지, 벡터 요소를 추출한다 |
| FR-12 | M | 암호화 PDF는 사용자에게 암호를 요청하고, 열람 불가 시 명확한 오류를 표시한다 |
| FR-13 | S | 텍스트 레이어가 없는 페이지를 감지하고 OCR로 대체 추출한다 |
| FR-14 | C | 100 MB 이상 대용량 PDF도 페이지 단위 지연 로딩으로 처리한다 |

### 3.2 레이아웃 분석 (FR-2x)

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-20 | M | 각 페이지를 블록 단위로 분할하고 유형을 분류한다: `title, heading, paragraph, list, caption, table, figure, formula, header, footer, footnote, reference` |
| FR-21 | M | 다단(2단 이상) 레이아웃에서 올바른 읽기 순서(reading order)를 산출한다 |
| FR-22 | M | 그림·표·수식 블록은 번역 대상에서 제외하되 캡션은 번역 대상에 포함한다 |
| FR-23 | M | 페이지 경계를 넘어 이어지는 문단을 하나의 번역 단위로 병합한다 |
| FR-24 | S | 인라인 수식·인용표기(`[12]`, `(Kim et al., 2024)`)·URL·코드 스팬을 감지해 플레이스홀더로 보호한다 |
| FR-25 | S | 분석 결과를 UI에 오버레이로 표시하고, 사용자가 블록 유형·번역 제외 여부를 수동 수정할 수 있다 |

### 3.3 번역 (FR-3x)

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-30 | M | vLLM 서버(OpenAI 호환 API)의 `qwen3.6-35B-A3B-NVFP4-fast` 모델로 영→한 번역을 수행한다 |
| FR-31 | M | 블록 단위 번역 시 앞뒤 문맥(직전 블록 원문·번역, 섹션 제목, 논문 초록 요약)을 프롬프트에 포함해 일관성을 확보한다 |
| FR-32 | M | 선택된 용어집의 관련 용어만 추려 프롬프트에 주입한다 (전체 용어집 주입 금지) |
| FR-33 | M | 여러 번역 단위를 동시(비동기·동시성 제한)로 요청하여 처리량을 확보한다 |
| FR-34 | M | 실패한 요청은 지수 백오프로 재시도(기본 3회)하고, 최종 실패 블록은 원문 유지 + 실패 표시한다 |
| FR-35 | M | 진행 중 취소가 가능하며, 취소 시 완료분은 보존된다 |
| FR-36 | S | 서버 연결 상태·모델명을 사전 점검(헬스체크)하고 UI에 표시한다 |
| FR-37 | C | 번역 스타일 프리셋(경어체 논문투/간결체), 사용자 지정 시스템 프롬프트를 지원한다 |

### 3.4 후처리 (FR-4x)

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-40 | M | 플레이스홀더를 원래 요소로 복원하고, 누락·중복 시 해당 블록을 재번역 큐에 넣는다 |
| FR-41 | M | 숫자·단위·참조번호가 원문과 일치하는지 검증한다 |
| FR-42 | M | LLM 출력에서 흔한 오류(원문 반복, 설명 덧붙임, 마크다운 장식, 빈 출력)를 감지·제거한다 |
| FR-43 | S | 용어집 강제 적용 모드: 번역문에서 용어가 지정 역어와 다르면 치환 또는 재번역한다 |

### 3.5 PDF 재조판·출력 (FR-5x)

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-50 | M | 원본 블록의 위치(bbox)에 번역문을 배치한 PDF를 생성한다. 그림·표·수식·배경은 원본 그대로 유지한다 |
| FR-51 | M | 한글 폰트(예: Noto Sans KR / Nanum 계열)를 서브셋 임베딩하여 어떤 환경에서도 동일하게 표시한다 |
| FR-52 | M | 번역문이 원문 박스보다 길 경우 단계적 맞춤 전략(폰트 축소 → 행간 축소 → 박스 확장 → 넘침 표시)을 적용한다 |
| FR-53 | M | 원본 스타일(굵게/기울임/크기 위계)을 번역문에 최대한 반영한다 |
| FR-54 | S | 출력 모드 2종: ① 번역 대체본(원문 자리에 번역) ② 대역본(원문 페이지+번역 페이지 교차) |
| FR-55 | C | 하이라이트·링크 등 원본 어노테이션 보존 |

### 3.6 용어집 (FR-6x)

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-60 | M | 용어집 CRUD (용어쌍: 원어, 역어, 분야, 우선순위, 대소문자 구분, 메모) |
| FR-61 | M | 복수 용어집 관리 및 번역 작업별 선택 적용 |
| FR-62 | S | CSV/TSV 가져오기·내보내기 |
| FR-63 | C | 번역 결과에서 미등록 빈출 용어 후보를 추출해 등록 제안 |

### 3.7 UI/작업 관리 (FR-7x)

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-70 | M | 원문/번역 페이지 대조 뷰 (좌우 또는 탭 전환, 페이지 동기 스크롤) |
| FR-71 | M | 작업 진행률 표시(단계·페이지·블록·예상 남은 시간)와 로그 뷰 |
| FR-72 | M | 설정 화면: vLLM 서버 URL/모델/API 키, 동시성, 폰트, 출력 옵션 |
| FR-73 | S | 최근 작업 목록과 작업 결과(IR·번역) 로컬 저장, 앱 재시작 후 이어보기 |
| FR-74 | C | 여러 PDF 배치(큐) 번역 |

## 4. 비기능 요구사항

### 4.1 성능 (NFR-1x)

| ID | 요구사항 |
|---|---|
| NFR-10 | 10페이지 일반 논문(텍스트 레이어 존재) 기준: 분석 ≤ 30초, 번역 전체 파이프라인 ≤ 5분 (vLLM 서버 성능 기준치는 부록 A에 정의) |
| NFR-11 | 분석·번역 중에도 UI는 응답성을 유지한다 (블로킹 금지, 모든 장시간 작업은 비동기) |
| NFR-12 | 앱(엔진 포함) 상주 메모리 ≤ 4 GB (레이아웃 모델 포함) |
| NFR-13 | LLM 요청 동시성은 설정 가능(기본 4)하며 서버 과부하 시 자동 감속(backpressure) |

### 4.2 신뢰성 (NFR-2x)

| ID | 요구사항 |
|---|---|
| NFR-20 | Python 엔진 프로세스 비정상 종료 시 Go가 감지·자동 재시작하고 진행 중 작업을 실패 처리 후 사용자에게 알린다 |
| NFR-21 | 번역 작업 상태를 주기적으로 로컬에 체크포인트하여 중단 후 이어하기가 가능하다 |
| NFR-22 | 어떤 입력 PDF도 앱 크래시를 유발해서는 안 된다 (파싱 실패는 오류 메시지로 처리) |

### 4.3 보안·프라이버시 (NFR-3x)

| ID | 요구사항 |
|---|---|
| NFR-30 | 문서 내용은 vLLM 서버 외부로 전송되지 않는다 (텔레메트리 없음) |
| NFR-31 | vLLM API 키(사용 시)는 OS 자격증명 저장소 또는 암호화 설정 파일에 저장 |
| NFR-32 | 사이드카 JSON-RPC는 stdio 전용으로 하여 외부 포트를 열지 않는다 |

### 4.4 사용성·기타 (NFR-4x)

| ID | 요구사항 |
|---|---|
| NFR-40 | UI 언어는 한국어 기본, 영어 선택 |
| NFR-41 | 설치는 단일 설치본으로 완결 (Python 런타임·모델 포함 번들, 별도 설치 불필요) |
| NFR-42 | 로그는 로컬 파일로 순환 저장(기본 7일)하며 개인 문서 내용은 로그에 남기지 않는다(옵션으로 디버그 로그) |

## 5. 제약사항

- C-01: LLM은 vLLM이 서빙하는 로컬/내부망 모델만 사용한다. (인터넷 상용 API 미사용)
- C-02: 데스크탑 앱은 Go + Wails v3.0으로 구현한다. Wails v3는 알파~베타 단계이므로 API 변경 리스크를 개발 계획의 리스크 항목으로 관리한다.
- C-03: 레이아웃 분석·번역·재조판 파이프라인은 Python 패키지 `translate_engine`으로 구현하고, 앱과는 사이드카 프로세스(stdio JSON-RPC)로 통신한다.
- C-04: 원본 PDF의 저작권을 존중한다 — 출력 PDF에 "기계번역본" 표시(속성 메타데이터)를 남기고, 원본 파일은 수정하지 않는다.
- C-05: PDF 재조판은 원본을 배경으로 유지하고 텍스트만 교체하는 방식(오버레이/리댁션)을 기본으로 한다. 완전한 리플로우 조판은 범위 외.

## 6. 수용 기준 (요약)

- 골든 PDF 세트(§`06-test-plan.md`) 20종에 대해: 블록 유형 분류 정확도 ≥ 95%, 읽기 순서 오류 페이지 ≤ 5%, 플레이스홀더 복원율 100%, 재조판 후 텍스트 넘침(잘림) 블록 0건(넘침 표시 처리 포함).
- UC-01 전체 흐름이 사용자 개입 없이 완료되고, 결과 PDF가 Adobe Acrobat / 브라우저 뷰어에서 정상 표시된다.

## 부록 A. 성능 기준 환경

- vLLM 서버: `qwen3.6-35B-A3B-NVFP4-fast`, 단일 노드 GPU 서빙, 동시 요청 4 기준 블록당 평균 응답 ≤ 8초를 가정. 실제 서버 성능이 다르면 NFR-10의 시간 목표를 비례 조정한다.
