# 05. 개발 계획서

## 1. 개발 전략

- **파이프라인 우선, UI 후행**: `translate_engine`을 CLI로 먼저 완성해 골든 PDF로 품질을 확립한 뒤 Wails 앱을 얹는다. 엔진은 앱 없이도 `python -m translate_engine translate in.pdf out.pdf` 형태로 실행 가능하게 유지한다(개발·디버깅·테스트 자동화에 계속 사용).
- **스키마 우선**: M1에서 IR·RPC 스키마를 확정하고, 이후 변경은 호환성 규칙(`04-api-spec.md` §4)을 따른다.
- **수직 슬라이스 마일스톤**: 각 마일스톤은 "특정 PDF 한 장이 끝까지 통과"를 완료 기준으로 삼는다.

## 2. 마일스톤

| MS | 기간(주) | 목표 | 완료 기준 (Definition of Done) |
|---|---|---|---|
| **M0** 준비 | 1 | 리포지토리·CI·스키마 초안, 골든 PDF 초기 10종 수집(M3까지 20종으로 확대 — `06-test-plan.md` §2), vLLM 접속 검증 | CI에서 Go/Python 빌드·린트 통과, `qwen.py` 스파이크로 샘플 문단 번역 확인 |
| **M1** 파싱·레이아웃 | 3 | `layout.py` 완성 (파서+레이아웃+읽기순서, OCR 제외) | 골든 셋 1단·2단 논문에서 블록 분류·읽기 순서 육안 검수 통과, IR 스키마 v1.0 확정 |
| **M2** 번역 파이프라인 | 3 | `terminology.py`, `qwen.py`, `translator.py` (후처리 포함) | CLI로 논문 1편 전체 번역, 플레이스홀더 복원율 100%, 체크포인트 재개 동작 |
| **M3** 재조판 | 2 | `renderer.py` (replace 모드) | 번역 PDF가 Acrobat/브라우저에서 정상 표시, 넘침 처리 동작, 한글 폰트 임베딩 확인 |
| **M4** 앱 통합 | 3 | Wails 앱 + 사이드카 + 핵심 UI (열기/진행률/대조 뷰/저장) | UC-01이 앱에서 끝까지 동작, 엔진 크래시 자동 재시작 확인 |
| **M5** 부가 기능 | 2 | 용어집 UI·CSV, 부분 재번역, OCR, interleaved 모드, 설정 화면 | FR의 M 항목 전부 + S 항목 80% 구현 |
| **M6** 안정화·패키징 | 2 | 3-OS 패키징·서명, 성능 튜닝, 테스트 계획 수행 | 수용 기준(`01-srs.md` §6) 충족, 설치본 3종 산출 |

총 16주 (1인 개발 기준. 병행 작업 여유 없으면 ±20%).

## 3. 마일스톤별 세부 작업 (발췌)

### M1 (layout)
- PyMuPDF 추출기 + 스팬 정규화 → DocLayout-YOLO ONNX 추론 래퍼 → IoU 정합 → 단 감지·XY-cut → continues 링크 → IR 직렬화·스키마 검증
- 산출물: `layout.py`, `ir.py`, 골든 셋 대상 시각화 디버그 도구(블록 오버레이 PNG 덤프)

### M2 (translate)
- Aho-Corasick 용어 인덱스 → TU 빌더(병합·분할·마스킹) → 프롬프트 빌더 → 비동기 디스패처(세마포어·재시도·backpressure) → 후처리 8단계 → 체크포인트/재개/취소
- 프롬프트 튜닝 사이클: 골든 문단 50개로 스타일·용어 준수율 평가 후 시스템 프롬프트 확정

### M4 (app)
- Wails v3 스캐폴드 → Supervisor+RPC 클라이언트 → 서비스 4종 → pdf.js 대조 뷰 → 진행률/이벤트 배선 → 잡 이력

## 4. 리스크 관리

| # | 리스크 | 확률 | 영향 | 대응 |
|---|---|---|---|---|
| R1 | **Wails v3 알파 API 변경/버그** | 높음 | 중 | 버전 고정, Wails 의존을 `app/services` 어댑터로 격리. 심각 시 v2로 다운그레이드 경로 확보(서비스 계층은 재사용) |
| R2 | 레이아웃 모델 정확도 부족(복잡한 2단·수식 밀집 논문) | 중 | 높음 | 후보 모델 2종(M0에서 벤치), 수동 수정 UI(FR-25)로 보완, 골든 셋 확대 |
| R3 | 한글 텍스트 넘침으로 조판 품질 저하 | 중 | 중 | 4단계 fitting 전략 + 골든 셋 캘리브레이션, 최악 시 interleaved 모드 권장 |
| R4 | vLLM 서버 성능/가용성 변동 | 중 | 중 | backpressure·체크포인트·재개로 흡수, 헬스체크로 사전 감지 |
| R5 | PyInstaller 번들 문제(모델 경로, 서명, AV 오탐) | 중 | 중 | M3부터 매 마일스톤 3-OS 패키징 스모크 테스트(뒤로 미루지 않음) |
| R6 | 모델 출력 변동(사고 모드 혼입, 형식 위반) | 중 | 낮음 | 후처리 방어층(§03 5.5) + 재번역 큐가 흡수. 프롬프트 회귀 테스트 |
| R7 | 배포본 크기 초과 | 낮음 | 낮음 | ONNX화, 폰트 서브셋, OCR 모델 지연 다운로드 옵션 |

## 5. 개발 환경·규약

- 리포지토리: 모노레포(§02 §4 구조). 브랜치: trunk 기반, 기능 브랜치 + PR
- CI: GitHub Actions — ① Go build/test/lint(golangci-lint) ② Python test/lint(ruff, mypy) ③ 스키마-타입 동기화 검사 ④ (main) 3-OS 패키징 스모크
- 커밋 전 로컬: `make check` (양쪽 린트+단위 테스트)
- 코드 규약: Go 표준 스타일, Python은 ruff 기본 + 타입힌트 필수(mypy strict는 `translate_engine` 핵심 모듈만)
- 버전: 앱과 엔진은 같은 버전 번호로 동시 릴리스(프로토콜 버전은 별도)

## 6. 산출물 목록

| 단계 | 산출물 |
|---|---|
| 설계 | 본 문서 세트, JSON Schema 3종 |
| M1~M3 | `translate_engine` 패키지 + CLI, 골든 셋 검증 리포트 |
| M4~M5 | 데스크탑 앱, 사용자 도움말(간단 README) |
| M6 | 설치본 3종, 테스트 결과서, 릴리스 노트 |
