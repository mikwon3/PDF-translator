# 06. 테스트 계획서

## 1. 테스트 전략 개요

| 레벨 | 대상 | 도구 | 자동화 |
|---|---|---|---|
| 단위 | `translate_engine` 각 모듈, Go 서비스·잡 상태머신 | pytest / go test | CI 매 PR |
| 통합 | 엔진 파이프라인(CLI), Go↔Python RPC, vLLM 연동 | pytest + 목 서버 | CI 매 PR (vLLM은 목), 나이틀리(실서버) |
| E2E | 앱 전체(UC-01~04) | Wails 앱 + Playwright(WebView) 또는 수동 시나리오 | 마일스톤 말 |
| 회귀 | 골든 PDF 세트 | 전용 러너 + 리포트 | 나이틀리 |
| 성능 | NFR-10~13 | 벤치 스크립트 | 마일스톤 말 |

원칙: **LLM 비결정성 격리** — CI 기본 경로에서는 vLLM을 목(mock) 서버(기록·재생 방식)로 대체하고, 실서버 테스트는 나이틀리·수동으로 분리한다. 번역 "품질" 평가와 파이프라인 "정확성" 검증을 구분한다.

## 2. 골든 PDF 세트 (20종)

| 분류 | 수량 | 예시 특성 |
|---|---|---|
| 1단 레이아웃 | 4 | 단순 워킹페이퍼, 프리프린트 |
| 2단 레이아웃 | 6 | IEEE/ACM 스타일, 수식·그림 다수 |
| 수식 밀집 | 3 | 수학/물리 논문, 인라인·디스플레이 수식 혼재 |
| 표 중심 | 2 | 대형 표, 다단 표 |
| 스캔본(OCR) | 2 | 텍스트 레이어 없음, 저품질 스캔 1 포함 |
| 특이 케이스 | 3 | 회전 페이지, 암호 PDF, 각주·2열 참고문헌 밀집 |

각 골든 PDF에는 기대값 파일을 둔다: `expected/<name>/blocks.json`(대표 페이지의 블록 유형·읽기 순서 정답), `expected/<name>/placeholders.json`.

## 3. 레벨별 테스트 설계

### 3.1 단위 테스트 (발췌)

| 모듈 | 주요 케이스 |
|---|---|
| `layout.py` | 스팬-블록 IoU 정합(경계값 0.5), 고아 스팬 흡수(텍스트 유실 0 보장), XY-cut 읽기 순서(2단·혼합단), continues 판정(미완결 문장/스타일 불일치 반례), 텍스트 밀도 기반 OCR 판정 |
| `terminology.py` | 최장일치 우선, 단어 경계, case_sensitive, 복수형 매칭, max_terms 절단 순서(priority→count), verify 위반 검출 |
| `translator.py` | TU 병합·분할(토큰 예산 경계), 마스킹/복원 왕복 항등성, 후처리 8단계 각각(원문 반복·placeholder 누락·길이 이상·think 태그 제거), 체크포인트 재개(중복 번역 없음), 취소 시 상태 보존 |
| `qwen.py` | 재시도·백오프(429/5xx/타임아웃), Retry-After 존중, 동시성 상한, backpressure 감속·복원, finish_reason=length 재시도, think 태그 방어 제거 |
| `renderer.py` | fitting 4단계 전이, min_font_scale 하한, 스타일 매핑(bold/italic/size), 실패 TU 하이라이트, 기계번역 라벨·메타데이터 |
| Go `jobs` | 상태머신 전이 전수(불법 전이 거부), 재개 조건, 체크포인트 원자성 |
| Go `engine` | RPC 프레이밍(분할 수신·대형 메시지), 크래시 감지→재시작 백오프, shutdown 에스컬레이션 |

### 3.2 통합 테스트

- **파이프라인 CLI**: 골든 PDF → analyze → translate(목 LLM) → render 전 구간. 검증: IR 스키마 유효성, 플레이스홀더 복원율 100%, 출력 PDF 열림(qpdf --check), 렌더 리포트 넘침 0.
- **레이아웃 정확도 회귀**: 골든 셋 각 대표 페이지의 analyze 결과를 `expected/<name>/blocks.json` 정답과 비교(블록은 IoU ≥ 0.7 매칭). 판정: 블록 유형 분류 정확도 ≥ 95%, 읽기 순서 오류(정답 순열 대비 인접 역전 존재) 페이지 비율 ≤ 5% — `01-srs.md` §6 수용 기준을 직접 측정.
- **목 LLM 서버**: OpenAI 호환 목 — ① 기록/재생 모드(실서버 응답 캡처를 fixture화) ② 고장 주입 모드(429 폭주, 타임아웃, 형식 위반 출력, `<think>` 혼입) — FR-34·후처리 경로 검증.
- **RPC 계약 테스트**: `rpc.schema.json` 기반으로 Go 클라이언트·Python 서버 양쪽에서 동일 fixture 검증(스키마 드리프트 검출).
- **사이드카 수명주기**: 엔진 강제 kill → Supervisor 재시작·잡 실패 처리·이어하기 제안 이벤트 확인.

### 3.3 E2E 시나리오 (마일스톤 말 수행)

| ID | 시나리오 | 합격 기준 |
|---|---|---|
| E2E-01 | UC-01 전체 흐름 (2단 논문 10p) | 개입 없이 완료, 출력 PDF 정상, 진행률·ETA 표시 |
| E2E-02 | 번역 중 취소 → 재개 | 완료분 보존, 재개 시 중복 요청 없음(목 서버 호출 수로 검증) |
| E2E-03 | vLLM 다운 상태에서 시작 | 헬스체크 실패 안내, 잡 시작 차단 |
| E2E-04 | 번역 중 vLLM 다운 | 잡 일시정지·안내, 서버 복구 후 재개 성공 |
| E2E-05 | 블록 수동 수정(FR-25) 후 번역 | 제외 블록 미번역, 유형 변경 반영 |
| E2E-06 | 부분 재번역·직접 수정(UC-03) | 해당 블록만 갱신, 저장 반영 |
| E2E-07 | 스캔 PDF(UC-04) | OCR 동의 흐름, 결과 정상 |
| E2E-08 | 암호/손상 PDF | 오류 UX 정상, 앱 크래시 없음 |
| E2E-09 | 앱 종료 중 잡 진행 | 정상 종료(엔진 shutdown), 재시작 후 이어하기 |
| E2E-10 | UC-02 용어집 관리 | 용어 CRUD·CSV 가져오기/내보내기 정상, 등록 용어가 다음 번역에 반영 |

### 3.4 성능 테스트 (NFR 검증)

- 기준 문서: 골든 셋 중 10p 2단 논문. 측정: 분석 시간, 번역 총시간, 피크 메모리(psutil), UI 응답성(주요 조작 < 100ms).
- 목 LLM(고정 지연 8초)로 파이프라인 오버헤드와 서버 시간을 분리 측정 — NFR-10 판정은 부록 A 기준 환경에서.
- 동시성 4/8/16 스윕으로 backpressure 동작·최적 기본값 확인.

## 4. 번역 품질 평가 (자동+수동)

자동 지표(나이틀리, 실서버):

- 플레이스홀더 복원율(=100% 필수), 숫자 일치율, 용어집 준수율(강제 모드 off 기준 ≥ 95%), 영문 잔존율(< 3% 문자), 재번역 큐 진입률(< 5%)
- 문장 수준 품질 회귀: 고정 문단 50개의 번역 결과를 기준 번역과 chrF로 비교 — 프롬프트/모델 변경 시 급락 감지용(절대 품질 지표가 아님을 명시)

수동 평가(마일스톤 말): 연구자 관점 체크리스트(용어 일관성, 문체, 수식 주변 자연스러움, 캡션 정확성)로 논문 2편 전수 검토, 5점 척도 평균 ≥ 4.0.

## 5. 재조판 품질 검증

- 자동: 렌더 리포트 기준 overflowed=0(주석 처리 포함), 폰트 임베딩 검사(pdffonts로 서브셋 확인), 텍스트 추출 왕복(출력 PDF에서 번역문 재추출 → translation.json과 일치)
- 시각 회귀: 골든 셋 각 페이지 렌더 PNG를 기준 이미지와 SSIM 비교(≥ 0.98), 차이 발생 시 diff 이미지 아티팩트 저장
- 뷰어 호환: Acrobat Reader, Chrome/Edge 내장 뷰어, macOS 미리보기에서 육안 확인(마일스톤 말)

## 6. 결함 관리·종료 기준

- 심각도: S1(크래시·데이터 손실) / S2(핵심 기능 불능) / S3(기능 결함·우회 가능) / S4(사소)
- 릴리스 종료 기준: S1·S2 = 0, S3 ≤ 5(문서화), 골든 셋 회귀 전체 통과, `01-srs.md` §6 수용 기준 충족
